# Marketing home

Mode: Persuade. Audience: consultant/implementer and agent power user; developer-with-own-API is a quieter third. Job: believe the same-day loop (server → tool → playground → agent URL) and Sign up. Proof: the gate cut, then a two-column cull — leftover REST vs the live bench at extreme scale. Constraints: brand manual (Ink, Paper, Gate Orange on the 2 only, Inter, system mono, custom wordmark); no invented testimonials, metrics, customers, or shipped-roadmap; hero composition stays as approved; no second scroll animation; no magazine serif/photo; no AI mascot/glow/glass. Approved hero comp: `.impeccable/mocks/decision/gate-cut.webp`. Approved below-fold: `.impeccable/mocks/below-fold-cull-scale.png` (cull + scale).

## Direction contract

THESIS: The home is the gate. After the cut, the page is a cull table: what does not pass on the left, the same-day objects at instrument scale on the right. It refuses the feature-card stack, equal marketing bands, and a second scroll theater.

OWN-WORLD: Paper `#FFFFFF` ground, Ink `#0B0B0B` type, Gate Orange `#FD410B` only on the 2. Inter for chrome. System mono for endpoints, curl, URLs, verdicts, tokens. Sharp rectangles, hairline rules, no glow, glass, or drop shadow. Traffic-light word `healthy` stays Ink, not green.

STORY: The visitor sees selection happen, then reads leftover inventory against a huge origin / curl→tool / healthy / connection slip, two equivalent doors, what stays closed, who it is for, what is not shipped, and Signs up.

FIRST VIEWPORT: Unchanged — the approved gate-cut hero. Wordmark lockup and Sign up / Log in at the end of that scroll.

FORM: Gate cut (seed `59744ca2`) continuing as cull list + playground verdict at scale. Below-fold topology: two columns, left reject, right live bench.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.
