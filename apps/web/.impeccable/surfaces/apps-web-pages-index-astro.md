---
version: 1
slug: "apps-web-pages-index-astro"
primary_target: "apps/web/pages/index.astro"
related_targets: ["apps/web/pages/es/index.astro"]
---

# Marketing home

Mode: Persuade. Audience: consultant/implementer and agent power user. Job: believe the same-day loop and Sign up. Proof: the gate cut, then two real agent chats — one authors a tool, an MCP card is born and physically moves into the specialist chat, where that tool does the job. Constraints: brand manual (Ink, Gate Orange, Inter, system mono, custom wordmark); reverse lockup on dark grounds; no invented testimonials, metrics, customers, or shipped-roadmap; hero composition and scroll animation stay; no magazine serif/photo; no AI mascot/glow/glass. User pins 2026-09-14: leave paper-flat; night console; ChatGPT conversational grammar with rest2mcp skin; richer MCP card that jumps to the specialist; code-first; identity for marketing and the SPA. Approved hero composition: `.impeccable/mocks/decision/gate-cut.webp` (animation unchanged).

## Direction contract

THESIS: After the gate, the page is a night workroom where you watch two agent chats. One conversation maps a REST call to a curated tool; a connection card is born; that card moves into a specialist chat that puts the tool to work. It refuses paper ledgers, feature-card stacks, and fake terminal transcripts.

OWN-WORLD: Night console. Ground `#0B0B0B`, lifted chat chassis `#161616`, luminous type `#EDEAE4`, Gate Orange `#FD410B` only on the 2, the Sign up action, the live lamp, the selected picker chip, and the MCP URL. Inter for chrome; system mono for code, URLs, tool names. ChatGPT grammar in rest2mcp skin: composer chrome, user bubble, thinking block, tool-call chips, assistant reply. Modest rounding on chat chrome (12px); hero stays sharp. Depth by stacked dark planes and offset shadows, not glow. The word `healthy` stays luminous, not green.

STORY: After the gate, a two-line definition names the service and the job. The visitor picks an orphan endpoint. Scroll plays the builder chat (natural request → thinking → tools → reply → MCP card), the card travels into the specialist chat, and that agent finishes the job. Below: a dump/pass cut, the four-step artifact rail, two console doors, a live/closed/not-yet board, who, FAQ, Sign up.

FIRST VIEWPORT: Unchanged gate-cut hero — same crop, same scroll choreography — on night ground with reverse wordmark, luminous hairlines, Gate Orange Sign up. Wordmark lockup and Sign up / Log in at the end of that scroll.

FORM: Night console + ChatGPT grammar. Seed `c9b81cd8` fused with user pins (dark, grammar-plus, card jump, hero stays, web+app, code-led). Signature interaction: the MCP card FLIPs from the builder transcript into the specialist transcript.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.
